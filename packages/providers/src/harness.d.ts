/**
 * Program Control harness marker for the W08 provider package.
 *
 * This declaration exists only so the package can participate in canonical
 * TypeScript gates before the first accepted W08 runtime leaf is added.
 * It emits no JavaScript and defines no provider, authority, credential,
 * execution, capability, or business semantics.
 */
export {};
