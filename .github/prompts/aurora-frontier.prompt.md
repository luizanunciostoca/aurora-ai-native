---
description: 'Recalculate and dispatch the maximum safe Aurora Pro+ parallel frontier'
---

Operate as `aurora-coordinator` / Global Integration Control Tower for `luizanunciostoca/aurora-ai-native`.

Revalidate live `main`, accepted exact-SHA evidence, issues/labels, canonical PRs/branches, active workers/workflows, wave dependency/ownership governance and shared-write locks before dispatch.

Reconstruct the live DAG as `ACCEPTED / BUILD_READY / INTEGRATION_READY / VALIDATION / PREBUILD / READINESS / BLOCKED`.

Then:

1. compute the longest remaining critical paths and dependency-unblocking value;
2. calculate dynamic safe canonical BUILD capacity from independent BUILD_READY nodes, exact path/semantic ownership, available isolated Copilot sessions, CI/Actions capacity and live account/runtime limits;
3. dispatch separate isolated sessions only for independent canonical tasks;
4. keep useful Puzzle READINESS/PREBUILD lanes active with `canonicalAuthority=false` and `requiresReconciliation=true`;
5. run read-only immutable-candidate integration/red-team/performance/scope audits concurrently where useful;
6. reject duplicate canonical ownership and all shared-surface write collisions;
7. after every accepted merge, post-merge verify and immediately recalculate the frontier.

Historical Free-mode `physicalBuildSlots=2` is not the current fixed Pro+ cap. Never widen Aurora dependency, authority, merge or acceptance rules because more agent capacity exists.

Report current main, active BUILD sessions, fleets, logical lanes, locks, validation queue, critical path, next promotions and `USER ACTION REQUIRED`.
