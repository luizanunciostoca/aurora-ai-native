name: W14 Interaction Gateway R2 Format

on:
  push:
    branches:
      - prebuild/w14-v017-interaction-gateway-adapter-r2

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: true
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22.16.0
          cache: npm
      - run: npm ci
      - name: Format reconciliation regression only
        shell: bash
        run: |
          set -euo pipefail
          npx prettier --write services/mobile-gateway/test/w14-interaction-gateway-reconciliation.test.ts
          git diff --check
          if git diff --quiet; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add services/mobile-gateway/test/w14-interaction-gateway-reconciliation.test.ts
          git commit -m 'style(W14): normalize gateway reconciliation regression'
          git push origin HEAD:prebuild/w14-v017-interaction-gateway-adapter-r2
